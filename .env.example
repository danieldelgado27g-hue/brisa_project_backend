# ──────────────────────────────────────────
# DermaMatch Backend — Variables de Entorno
# ──────────────────────────────────────────

# Node
NODE_ENV=development
PORT=3000

# PostgreSQL (local / Docker Compose)
POSTGRES_USER=brisa
POSTGRES_PASSWORD=brisa123
POSTGRES_DB=brisa_db
POSTGRES_HOST=db
POSTGRES_PORT=5432
POSTGRES_HOST_PORT=5433  # Puerto del host (cambia si 5432 ya está ocupado)

# Netlify / Neon (producción)
# DATABASE_URL=postgresql://user:pass@ep-xxx.us-east-2.aws.neon.tech/neondb?sslmode=require
